import { AsideMenuDirective } from './aside-menu.directive';

describe('AsideMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideMenuDirective();
    expect(directive).toBeTruthy();
  });
});
